# Dependencies
/node_modules

# Production build
/build
/.docusaurus
.cache-loader

# Generated API reference (produced by DocFX + DocFxMarkdownGen at build time).
# The landing page (index.md) is committed so the API sidebar is never empty;
# everything else under docs/api is generated and ignored.
/docs/api/**
!/docs/api/index.md

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
